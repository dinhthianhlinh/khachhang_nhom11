<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Đường viền (border) -->
    <stroke
        android:width="1dp"
        android:color="@color/white" />

    <!-- Màu nền -->
    <size android:height="300dp" android:width="3800dp"/>

    <!-- Góc cong của khung -->
    <corners
        android:radius="20dp" />
    <gradient android:startColor="#0627F4" android:endColor="#0C0627F4" android:centerColor="#880627F4"/>

</shape>
